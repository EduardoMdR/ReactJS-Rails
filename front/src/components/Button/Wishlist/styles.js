import styled from 'styled-components';

export const Container = styled.button`
  background: linear-gradient(-45deg, #0a75ad, #7182e0);
  height: 2.5rem;
  outline: none;
  border: none;
  color: #FFF;
  font-weight: bold;
  font-size: 0.9rem;
  transition: 0.4s;
  border-radius: 5px;
  &:hover {
    cursor: pointer;
  }
`;